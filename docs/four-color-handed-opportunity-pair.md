# Handed-Off Opportunity Pair

**Status:** exact current-state refinement of opportunity totality.

A positive focus admissibility set

\[
A(v)=Q_4\setminus c(N(v))\neq\varnothing
\]

authorizes a **focus color commitment**.  It is not a stop action.  The Four
Color proof surface therefore does not identify `A(v) != empty` with stopping.

If instead zero focus slack persists after one realized dual-domain action,
the actual successor supplies exactly two singleton directions.  One is the
source direction sigma, which remains singleton.  The other is the
**resulting other singleton direction** tau, and tau lies in the source shared
opportunity pair:

\[
\tau\in O_\sigma.
\]

Current opportunity totality already derives four current controls at that
successor: two in sigma and two in tau.  The handed-off refinement discards the
sigma pair and retains only the two controls in tau:

\[
\boxed{
A(v)=\varnothing
\Longrightarrow
|\mathcal H(z')|=2,
\qquad
\mathcal H(z')=
\{T\in\mathcal A_{\rm dual}(z'):\operatorname{mode}(T)=\tau\}.
}
\]

`HandedOffOpportunityPairCertificate` carries no future route and no preferred
member of the pair.  It records only the direction supplied by the actual
successor and the two embedding-derived controls that currently share that
opportunity.

The pair is therefore a present permission object, not two realized
consequences.
