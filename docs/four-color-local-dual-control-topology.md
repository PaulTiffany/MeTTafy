# Local Dual-Control Topology Delta

**Status:** exact one-step accounting law banked. This is a present-control
certificate, not a future-route claim.

The retained degree-five phase identity is

\[
\Phi = 10 + 2C - S,
\]

with \(C\) the total internal alternating-cycle count and \(S\) the number of
two-edge terminal paths across the three complementary V4 networks.

A certified dual path switch preserves the same pentagonal carrier. Therefore
for every currently applicable exact dual control,

\[
\boxed{\Delta\Phi = 2\Delta C - \Delta S.}
\]

`DualControlTopologyDeltaCertificate` derives the exact successor through the
existing alternating-path switch certificate, independently certifies the
before/after phase topology, and checks the boxed identity.

On the three-interior pivot witness all four current controls satisfy

\[
\Delta C=-1,
\qquad
\Delta S=0,
\qquad
\Delta\Phi=-2.
\]

After any such first stage, the actual successor recomputes its own current
controls. The mechanical witness then exposes different lawful consequences
from that same present state: one control can return to a higher pivot phase,
while another reaches direct control geometry with no scalar phase change.

This is the useful distinction:

\[
\boxed{\text{applicability is present; consequence is control-dependent}.}
\]

No global reachability variable is introduced. The control problem is local:
derive the controls that exist now, certify their exact \((\Delta C,\Delta S)\),
act, and re-derive from the successor.
